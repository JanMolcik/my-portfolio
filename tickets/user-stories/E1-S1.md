---
id: E1-S1
type: user_story
title: As a developer, I want Next.js App Router initialized in this repo from the local TS blueprint baseline, so that implementation can start in-place.
status: done
priority: P1
owner: unassigned
invariants: [INV-5]
depends_on: []
source: docs/spec/EPICS_AND_USER_STORIES.md
---

## Goal
- As a developer, I want Next.js App Router initialized in this repo from the local TS blueprint baseline, so that implementation can start in-place.

## Scope
- Delivery aligned with G-A execution group.

## Non-goals
- Unrelated module rewrites.

## Acceptance
- App boots locally from `blueprints/blueprint-core-nextjs-ts`; `app/` and `src/` structure matches `ARCHITECTURE.md`; no Gatsby runtime path exists; baseline `typecheck`, `lint`, and `build` pass.
